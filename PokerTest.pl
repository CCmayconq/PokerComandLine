#!/usr/bin/perl
use Win32::Console::ANSI;
use Term::ANSIColor qw(:constants);
use POSIX qw(locale_h);
use locale;

#Simulador de Poker via linha de comando (Colorido)
#Maycon Q

print RED chr(3), chr(4), RESET,chr(5), chr(6);
